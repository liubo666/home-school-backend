# 环境变量配置模板
# 复制此文件为 .env 并填入实际配置值
# 生产环境请使用真实的值，不要使用默认值

# ========================================
# 数据库配置 (必需)
# ========================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=home_school
DB_USERNAME=root
DB_PASSWORD=

# 数据库SSL配置 (生产环境建议设置为true)
USE_SSL=false

# Flyway数据库迁移配置 (如果使用Flyway)
FLYWAY_URL=jdbc:mysql://localhost:3306/home_school
FLYWAY_USER=root
FLYWAY_PASSWORD=

# ========================================
# Redis配置 (可选)
# ========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0

# ========================================
# JWT配置 (必需)
# ========================================
# JWT密钥必须至少32字符，生产环境请使用强密码
JWT_SECRET=
JWT_EXPIRATION=86400000

# ========================================
# MinIO配置 (文件存储，可选)
# ========================================
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=home-school

# ========================================
# 应用配置
# ========================================
# 激活的环境：dev/test/prod
SPRING_PROFILES_ACTIVE=dev

# 应用端口和上下文路径
APP_PORT=8089
APP_CONTEXT_PATH=/api

# ========================================
# 安全配置 (可选)
# ========================================
# CORS允许的来源，多个用逗号分隔
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# ========================================
# 文件存储配置 (可选)
# ========================================
STORAGE_TYPE=local
LOCAL_BASE_PATH=/var/home-school/files

# ========================================
# 日志配置 (可选)
# ========================================
# 日志文件路径
LOG_FILE_PATH=/var/log/home-school/backend.log